When restarting a file, re-read the *vt files and remove doubled
entries.

Add RK4/5 method.

Add higher-order Strang splitting.

Add DG method.

check convergence of output in terms of FFT convergence
